# Calendar Bundle Configuration
# This file is automatically loaded by Symfony Flex

calendar:
    theme: bootstrap
    assets:
        include_cdn: false
    route_prefix: /calendar
    views:
        enabled: [month]
        default: month
    features:
        all_day_events: true
        colors: true
