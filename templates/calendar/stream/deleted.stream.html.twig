{# Supprimer l'événement du calendrier #}
<turbo-stream action="remove" target="event-{{ event.id }}">
</turbo-stream>

{# Fermer le modal #}
<turbo-stream action="update" target="event-modal">
    <template></template>
</turbo-stream>
