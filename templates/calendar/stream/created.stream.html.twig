{# Fermer le modal d'abord #}
<turbo-stream action="update" target="event-modal">
    <template></template>
</turbo-stream>

{# Recharger la frame du calendrier via Turbo #}
<turbo-stream action="update" target="calendar" source="{{ path('calendar_month', {year: year, month: month}) }}">
</turbo-stream>
